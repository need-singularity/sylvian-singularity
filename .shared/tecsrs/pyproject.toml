[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "tecsrs"
version = "0.1.0"
description = "TECS-L Rust acceleration: sieves, search, Monte Carlo, grid, ODE, atlas"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
